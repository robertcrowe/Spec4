"""Deprecated: browser preferences are now handled by dcc.Store(storage_type='local').

The prefs store (id='prefs') in app.py persists: provider, api_key, model, tavily_key.
This module is retained as a stub so existing imports do not break.
"""
